<!-- <% if @student.errors.any? %>
  <div class="container">
    <div class="row sign-up-form">
      <div class="col s12 m12 l12">
        <div class="card-panel orange lighten-2" id="fade-out-target">
          <span class="white-text">
            <strong>
              <%= pluralize(@student.errors.count, "error") %>
              prohibited your account from being created
              <ul>
                <% @student.errors.full_messages.each do |message| %>
                <li><%= message %></li>
                <% end %>
              </ul>
            </strong>
          </span>
        </div>
      </div>
    </div>
  </div>
<% end %>

<div class="container container-sign-up-form">
  <div class="row center">
    <div class="col s12 m12 l12">
      <div class="card-panel">
        <h3 class="center-align">Edit Your Profile</h3>
        <div class="row">
          <%= form_for(@student, html: {class: "col s12"}) do |f| %>
          <div class="row">
            <div class="input-field col s12">
              <%= f.text_field :name, placeholder: "Enter your full name", autofocus: true %>
              <%= f.label :name %>
            </div>
          </div>
          <div class="row">
            <div class="input-field col s12">
              <%= f.email_field :email, placeholder: "Enter your email address" %>
              <%= f.label :email %>
            </div>
          </div>
          <div class="row">
            <div class="input-field col s12">
              <%#= f.submit "SAVE CHANGES", class: "btn waves-effect waves-light right" %>
              <%= f.button "SUBMIT", class: "btn waves-effect waves-light right", data: {disable_with: "<i class='fa fa-spinner fa-spin'></i>  saving changes..." } %>
            </div>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div> -->

<%= render 'form' %>